<%- include('parser/header'); -%>
    <h1>HomePage</h1>
    <p><%= homeStartingContent %></p>
    
    <% posts.forEach(function(post){ %>
        <h1><%= post.title %></h1>
        <p><%=post.body.substring(0,100)+"....."%>
            <a href="/post/<%= post.title %>">Read More</a>
        </p>
        
    <%})%>
<%-include('parser/footer');-%>
